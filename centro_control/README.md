# Centro de Control

Aplicacion web externa para subir exportaciones CSV/XLSX, validarlas y ejecutar el procesamiento existente.

Esta carpeta contiene la futura interfaz Flask. El motor de analisis permanece en `scripts/` para evitar duplicar logica.

Ejecucion prevista:

```powershell
python centro_control/app.py
```
